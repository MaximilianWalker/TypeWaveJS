# Set registry for scoped packages
@typewavejs:registry=https://registry.npmjs.org/

# Strict SSL for increased security
strict-ssl=true

# Save dependencies with exact versions rather than using npm's default semver range
save-exact=true

# Optional: configure more verbose logging if needed
loglevel=info